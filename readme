Для запуска необходимо:
    64-разрядная система(запускаемые по умолчанию вебдрайверы 64-разрядные);
    установленная JDK версии 1.8.0 или выше;
    установленная IntelliJ IDEA последней версии;
    версия InternetExplorer 11.0 и выше, Google Chrome (вспомогательный) 56 и выше (на более ранних не тестировалось)(полностью рабочая конфигурация: версия  IE -11.0.9600.18537; версия Google Chrome - Версия 57.0.2987.133 (64-bit));
    установленные сертификаты, которыми будет подписан документ;
    установленный CryptoPro CSP;
    доступ к сети Интернет.
Требования InternetExplorerDriver для запуска (без их выполнений тесты могут не проходить, или проходить частично/неправильно):
    На ОС Windows Vista или Windows 7 уровни безопасности для каждой зоны должны быть установлены на одинаковый уровень.
    Для этого в запущенном InternetExplorer открыть "Свойства браузера"->"Безопасность" и настроить уровень для каждой зоны. Здесь же необходимо отключить защищенный режим.
Кроме того, необходимо внести изменения в реестр, а именно:
 Для 32-bit Windows версий, ключ для изменения находится по адресу:
 HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Internet Explorer\Main\FeatureControl\FEATURE_BFCACHE.
 Для 64-bit Windows версий, ключ:
 HKEY_LOCAL_MACHINE\SOFTWARE\Wow6432Node\Microsoft\Internet Explorer\Main\FeatureControl\FEATURE_BFCACHE.
 Подключ FEATURE_BFCACHE может быть или не быть показан. Если он не показан, его нужно создать.
 Важно: внутри этого ключа создайте переменную DWORD с именем iexplore.exe со значением 0.

Для запуска необходимо в классе Main.java запустить метод main(), кликнув по нему ПКМ и выбрав Run'main()'.
Примечание: обычно тесты начинают работать корректно после 3-4 запуска и далее работают без сбоев.


